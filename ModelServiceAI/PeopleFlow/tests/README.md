📂 Project Structure & Tests
Раздел тестирования и демонстрации работы модели.

Plaintext
tests/
├── data/               # Медиа-семплы (видео .mp4, изображения)
└── socket_test.py      # Клиент для теста WebSocket + YOLO
Описание компонентов
tests/data/ — Семплы для захвата видеопотока. Позволяют тестировать детекцию без физической камеры.

tests/socket_test.py — Эмулятор клиента. Читает видео, нарезает на кадры и стримит их на сервер через WebSockets, проверяя ответы модели.

🚀 Quick Start (Testing)
Чтобы проверить работу связки FastAPI + YOLO, выполните следующие шаги:

1. Запуск сервера
Из корня проекта:

Bash
uvicorn main:app --reload
2. Запуск теста
Откройте новый терминал и запустите скрипт (обработка первых 10 секунд видео):

Bash
python -m tests.socket_test
